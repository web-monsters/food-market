const nuxtServerInit = async ({commit, getters},
  {$axios, res,  req, app, isMobile, isTablet}) => {
};

export const state = () => ({
});

export const mutations = {
};

export const actions = {
  nuxtServerInit,
};

export const getters = {
};
